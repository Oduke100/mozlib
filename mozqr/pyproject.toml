[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mozqr-lib"
version = "0.1.0"
description = "My own QR code library"
authors = [{ name = "Oduke", email = "odukem622@gmail.com" }]
requires-python = ">=3.8"
dependencies = ["qrcode[pil]"]
